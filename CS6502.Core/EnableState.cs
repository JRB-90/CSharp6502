namespace CS6502.Core
{
    /// <summary>
    /// Enum to make enable states less ambiguous for pins that could be active low.
    /// </summary>
    public enum EnableState
    {
        Enabled,
        Disabled
    }
}
